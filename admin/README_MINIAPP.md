# Mini App Админ Панель - Полная Инструкция

## 📋 Что это?

Функциональность для просмотра участников разных чатов (Клан, 1-й состав, 2-й состав) и их разрешений команд через красивый Telegram Mini App, вместо стандартной админ-панели.

## 📁 Структура файлов

```
admin/
├── admin_api.py                    # REST API сервер для Mini App
├── admin_integration.py            # Интеграция API с админ-ботом
├── ADMIN_API_README.md             # Подробное описание API
├── INTEGRATION_EXAMPLE.py          # Примеры интеграции
├── admin_bot.py                    # Основной админ-бот (обновите этот файл)
└── app/
    └── index.html                  # Telegram Mini App интерфейс
```

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
# aiohttp уже в requirements.txt, просто убедитесь что установлены все зависимости
pip install -r requirements.txt
```

### Шаг 2: Обновление admin_bot.py

Откройте [admin_bot.py](admin_bot.py) и добавьте эти строки в начало файла (после импортов):

```python
from admin_config import *
from admin_integration import start_api_server

print('start')

# ✨ Добавите эти строки ✨
try:
    if can_admin_panel:
        admin_id = can_admin_panel[0]
        start_api_server(user_id=admin_id, host='0.0.0.0', port=8080)
        print(f'✓ API сервер запущен на http://0.0.0.0:8080')
except Exception as e:
    print(f'✗ Ошибка запуска API: {e}')
# ✨ До этого места ✨

# Остальной код...
```

### Шаг 3: Запуск админ-бота

```bash
python admin_bot.py
```

Вы должны увидеть:
```
start
✓ API сервер запущен на http://0.0.0.0:8080
```

### Шаг 4: Конфигурация Mini App

Найдите в файле [admin/app/index.html](app/index.html) строку:

```javascript
const API_BASE_URL = 'http://localhost:8080/api'; // Измените на ваш адрес
```

**Для локального тестирования:** оставьте как есть

**Для production:**
```javascript
const API_BASE_URL = 'https://your-domain.com/api'; // Ваш домен
```

## 📱 Использование Mini App

### Через Telegram бота

1. Отправьте `/start` админ-боту
2. Нажмите кнопку "Админ - панель" (или добавьте новую кнопку "📱 Mini App")
3. Откроется красивый интерфейс Mini App

### Через веб-интерфейс (для тестирования)

```bash
# Откройте в браузере
http://localhost:8000/admin/app/index.html

# Или с простым HTTP сервером
cd admin/app
python -m http.server 8000
```

## 🎨 Функциональность Mini App

### Просмотр участников
- **1️⃣ Первый Состав** - Список участников 1-го состава
- **2️⃣ Второй Состав** - Список участников 2-го состава  
- **🏰 Клан** - Список участников клана

Для каждого участника показывается:
- Номер в списке
- Ник/username (ссылка на Telegram профиль)
- Имя, возраст
- PUBG ник и ID
- Ранг и должность
- Количество сообщений

### Просмотр разрешений (ДК)
- **Клан ДК** - Разрешения команд в клане
- **Состав ДК** - Разрешения команд в составе

Для каждой команды показывается:
- Название команды (бан, мут, варн и т.д.)
- Статус доступа (✓ Есть / ✗ Нет)

## 🔒 Безопасность

### Проверка доступа

API автоматически проверяет:
1. Входит ли пользователь в список `can_admin_panel` из config
2. Имеет ли валидный Telegram Mini App token

### Доступ разрешен только для:
```python
can_admin_panel = [8015726709, 1401086794, 1240656726]
```

Если пользователя нет в списке, он увидит сообщение: "❌ Доступ запрещен"

## 📡 API Endpoints

### Проверка доступа
```bash
curl -X POST http://localhost:8080/api/check-access \
  -H "Content-Type: application/json" \
  -d '{"user_id": 123456789}'
```

### Получение участников
```bash
curl http://localhost:8080/api/users/klan
curl http://localhost:8080/api/users/sost-1
curl http://localhost:8080/api/users/sost-2
```

### Получение разрешений
```bash
curl http://localhost:8080/api/permissions/klan
curl http://localhost:8080/api/permissions/sost-1
curl http://localhost:8080/api/permissions/sost-2
```

## 🛠️ Конфигурация для Production

### 1. HTTPS и Domén

```javascript
// admin/app/index.html
const API_BASE_URL = 'https://chat-bot.example.com/api';
```

### 2. Nginx конфигурация

```nginx
server {
    listen 443 ssl http2;
    server_name chat-bot.example.com;
    
    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/chat-bot.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chat-bot.example.com/privkey.pem;
    
    # API прокси
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Mini App статические файлы
    location /admin/ {
        alias /home/user/chat_manager_bot/admin/app/;
        expires 1h;
        add_header Cache-Control "public, max-age=3600";
    }
}
```

### 3. Systemd сервис (опционально)

```ini
# /etc/systemd/system/admin-bot.service
[Unit]
Description=Admin Bot API Service
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/user/chat_manager_bot
ExecStart=/usr/bin/python3 admin/admin_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

## 🐛 Отладка

### Проверка что API работает

```bash
# Проверим что API слушает порт
netstat -tlnp | grep 8080

# Или
ss -tlnp | grep 8080
```

### Проверка через curl

```bash
# Должно вернуть JSON с ошибкой доступа (если пользователь не в списке)
curl http://localhost:8080/api/users/klan

# Проверка конкретного пользователя
curl -X POST http://localhost:8080/api/check-access \
  -H "Content-Type: application/json" \
  -d '{"user_id": 1240656726}'
```

### Логирование

Все запросы и ошибки выводятся в консоль при запуске бота.

## 📊 Структура базы данных

API работает с существующей структурой базы данных:

```
Base_bot.db:
├── [-klan]          # Таблица участников клана
├── [-sost_1]        # Таблица участников 1-го состава
├── [-sost_2]        # Таблица участников 2-го состава
├── [klan]           # Таблица разрешений клана
├── [sostav]         # Таблица разрешений составов
└── count_users      # Таблица с количеством пользователей
```

## 🎯 Команды поддерживаемые ДК (Доступ к Командам)

- `ban` - Блокировка пользователей
- `mut` - Ограничение пользователей (мут)
- `warn` - Предупреждение пользователей
- `all` - Созыв пользователей (@all)
- `rang` - Изменение ранга пользователей
- `dk` - Управление доступом к командам
- `change_pravils` - Изменение правил чата
- `close_chat` - Ограничения чата
- `change_priv` - Изменение приветствия
- `obavlenie` - Создание объявлений
- `tur` - Создание турниров
- `dell` - Удаление сообщений

## 👥 Ранги участников

0. Обычный участник
1. Младший Модератор
2. Модератор
3. Старший Модератор
4. Заместитель
5. Менеджер
6. Владелец

## ❓ Часто задаваемые вопросы

**Q: Как изменить порт API?**
A: В `start_api_server(port=9000)` или в переменной окружения.

**Q: Как добавить нового администратора?**
A: Добавьте его ID в `can_admin_panel` в [admin_config.py](admin_config.py)

**Q: Может ли быть несколько чатов помимо Клана и Состава?**
A: Да, отредактируйте `CHATS` в [admin_api.py](admin_api.py) и добавьте кнопки в HTML.

**Q: Почему "Доступ запрещен"?**
A: Проверьте что ваш Telegram ID в списке `can_admin_panel`.

**Q: Как использовать с Flask вместо aiohttp?**
A: Смотрите примеры в [INTEGRATION_EXAMPLE.py](INTEGRATION_EXAMPLE.py)

## 📚 Дополнительные ресурсы

- [API документация](ADMIN_API_README.md)
- [Примеры интеграции](INTEGRATION_EXAMPLE.py)
- [Telegram Mini Apps документация](https://core.telegram.org/bots/webapps)
- [aiohttp документация](https://docs.aiohttp.org/)

## 🤝 Поддержка

Если возникают проблемы:
1. Проверьте логи при запуске `python admin_bot.py`
2. Убедитесь что порт 8080 не занят
3. Проверьте что версия Python >= 3.8
4. Убедитесь что установлены все зависимости

## 📝 Лицензия

Часть проекта chat_manager_bot
