# 🚀 Настройка Mini App для GitHub Pages

## 📍 Ваш адрес Mini App
```
https://ivansalou288-tech.github.io/chat_manager_bot/admin/app/index.html
```

## ⚙️ Конфигурация (3 шага)

### Шаг 1: Загрузите HTML файл на GitHub Pages

1. Обновленный файл `admin/app/index.html` уже готов
2. Закоммитьте и запушьте его на GitHub:
```bash
git add admin/app/index.html
git commit -m "Update Mini App for GitHub Pages"
git push
```

3. Дождитесь деплоя (обычно 1-2 минуты)
4. Проверьте доступность по адресу выше

### Шаг 2: Запустите API сервер

API сервер **ДОЛЖЕН** быть запущен на вашем сервере. Это НЕ статический файл.

```bash
python admin/admin_bot.py
```

Вы увидите:
```
✓ API сервер запущен на http://0.0.0.0:8080
📱 Mini App доступен по адресу: https://ivansalou288-tech.github.io/chat_manager_bot/admin/app/index.html
```

### Шаг 3: Обновите API URL в index.html

**Вариант A: На локальной машине (для тестирования)**

В `admin/app/index.html` найдите строку ~750:
```javascript
const API_BASE_URL = 'https://your-api-server.com/api';
```

Измените на:
```javascript
const API_BASE_URL = 'http://localhost:8080/api';
```

**Вариант B: На production сервере (VPS/облако)**

Если API запущен на отдельном сервере (например, VPS):
```javascript
const API_BASE_URL = 'https://your-domain.com/api';
```

Например:
```javascript
const API_BASE_URL = 'https://api.mybot.com/api';
```

⚠️ **ВАЖНО:** API сервер должен быть доступен по HTTPS для работы с GitHub Pages!

## 🌐 Варианты развертывания API

### Вариант 1: На Localhost (локальное тестирование)
- ✅ Быстро
- ❌ Работает только если Mini App открыта на той же машине
- ❌ Не работает с GitHub Pages на публике

**API URL:** `http://localhost:8080/api`

### Вариант 2: На собственном VPS
- ✅ Работает со всеми клиентами
- ✅ Можно настроить HTTPS через Let's Encrypt
- ❌ Нужен свой сервер

**API URL:** `https://your-domain.com/api`

Примеры VPS: DigitalOcean, Linode, AWS, Azure

### Вариант 3: Через туннель (Ngrok)
- ✅ Быстро для тестирования
- ❌ Временный URL, нестабильно для production

```bash
# Установите ngrok
# https://ngrok.com

ngrok http 8080
# Вы получите: https://xxxx-xx-xxx-xxx.ngrok.io
```

Затем в index.html:
```javascript
const API_BASE_URL = 'https://xxxx-xx-xxx-xxx.ngrok.io/api';
```

## ✅ Проверка работоспособности

1. **Откройте Mini App в браузере:**
   ```
   https://ivansalou288-tech.github.io/chat_manager_bot/admin/app/index.html
   ```

2. **Проверьте консоль (F12 → Console):**
   - Если видны ошибки сети - проверьте API_BASE_URL
   - Если ошибка доступа - проверьте ID администратора

3. **Проверьте API напрямую:**
   ```bash
   curl https://your-api-url/api/users/klan
   ```

## 🔧 Используемые порты

- **8080** - API сервер (по умолчанию)
- Если 8080 занят, измените в admin_bot.py:
  ```python
  start_api_server(user_id=admin_id, host='0.0.0.0', port=9000)
  ```
  И в index.html:
  ```javascript
  const API_BASE_URL = 'http://your-domain.com:9000/api';
  ```

## 📚 Дополнительная информация

- Документация API: `admin/ADMIN_API_README.md`
- Интеграция: `admin/admin_integration.py`
- Конфигурация: `admin/admin_config.py`

## 🆘 Решение проблем

**Проблема:** Ошибка "Failed to fetch" в консоли
**Решение:** Проверьте что API_BASE_URL правильный и API сервер запущен

**Проблема:** CORS ошибки
**Решение:** API поддерживает CORS для GitHub Pages

**Проблема:** "Доступ запрещен"
**Решение:** Ваш ID должен быть в списке `can_admin_panel` в `admin_config.py`

---

**Готово! Mini App работает на GitHub Pages! 🎉**
