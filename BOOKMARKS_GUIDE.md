# 📌 Модуль закладок (Bookmarks Module)

## Описание

Модуль закладок позволяет пользователям сохранять сообщения из чатов путём ответа на них со значком 📌. Сохраненные закладки хранятся в отдельной таблице `Base_bot.db` с сохранением `message_id` для навигации.

## Функциональность

### Основные команды

#### 1. **Добавление закладки** - Ответ на сообщение с эмодзи 📌
```
📌
```
**Как использовать:**
- Ответьте на сообщение, которое хотите сохранить
- Напишите только эмодзи: 📌
- Сообщение будет сохранено в ваши закладки

**Пример:**
```
[ответ на сообщение]
📌
```

**Ответ бота:**
- ✅ Закладка добавлена! (если успешно)
- ⚠️ Эта закладка уже сохранена! (если уже существует)

#### 2. **Просмотр всех закладок** - команда `/bookmarks` или `/закладки`
```
/bookmarks
или
/закладки
```

**Функциональность:**
- Показывает список всех ваших закладок
- Для каждой закладки отображается:
  - 📌 Текст сообщения (первые 30 символов)
  - Имя автора сообщения
- Для каждой закладки доступны кнопки:
  - **кнопка закладки** - перейти к сообщению (получить копию в ЛС)
  - **❌** - удалить закладку

**Ответ бота при отсутствии закладок:**
```
📌 У тебя нет закладок

Чтобы добавить закладку, ответь на сообщение сообщением 📌
```

### Inline кнопки

**Переход к закладке:**
- Нажимаете кнопку с текстом закладки
- Сообщение форвардится вам в личные сообщения
- Бот отправляет уведомление: ✅ Сообщение отправлено в личные сообщения

**Удаление закладки:**
- Нажимаете кнопку ❌
- Закладка удаляется
- Список закладок автоматически обновляется

## Структура базы данных

### Таблица `bookmarks`

```sql
CREATE TABLE bookmarks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,          -- ID пользователя, который сохранил закладку
    chat_id INTEGER NOT NULL,          -- ID чата, где находится сообщение
    message_id INTEGER NOT NULL,       -- ID сообщения в чате (для навигации)
    message_text TEXT,                 -- Текст или превью сообщения
    author_id INTEGER,                 -- ID автора оригинального сообщения
    author_name TEXT,                  -- Имя/юзернейм автора
    created_at TIMESTAMP,              -- Время создания закладки
    UNIQUE(user_id, chat_id, message_id) -- Уникальность по пользователю, чату и сообщению
)
```

## API класса BookmarkManager

### Инициализация
```python
from modules.bookmarks import BookmarkManager

# С путём по умолчанию
bookmark_manager = BookmarkManager()

# С пользовательским путём к БД
bookmark_manager = BookmarkManager(db_path="/path/to/database.db")
```

### Методы

#### 1. `init_db()`
Инициализирует таблицу закладок в базе данных.
```python
bookmark_manager.init_db()
```

#### 2. `add_bookmark(user_id, chat_id, message_id, message_text=None, author_id=None, author_name=None)`
Добавляет новую закладку.

**Параметры:**
- `user_id` (int): ID пользователя
- `chat_id` (int): ID чата
- `message_id` (int): ID сообщения
- `message_text` (str, опционально): Текст сообщения
- `author_id` (int, опционально): ID автора сообщения
- `author_name` (str, опционально): Имя автора

**Возвращает:** `bool` - `True` если успешно, `False` если закладка уже существует

**Пример:**
```python
success = bookmark_manager.add_bookmark(
    user_id=123456,
    chat_id=-1001234567890,
    message_id=999,
    message_text="Важное сообщение",
    author_id=111111,
    author_name="@username"
)
```

#### 3. `remove_bookmark(user_id, chat_id, message_id)`
Удаляет закладку.

**Параметры:**
- `user_id` (int): ID пользователя
- `chat_id` (int): ID чата
- `message_id` (int): ID сообщения

**Возвращает:** `bool` - `True` если удалена, `False` если не найдена

**Пример:**
```python
deleted = bookmark_manager.remove_bookmark(
    user_id=123456,
    chat_id=-1001234567890,
    message_id=999
)
```

#### 4. `get_user_bookmarks(user_id)`
Получает все закладки пользователя.

**Параметры:**
- `user_id` (int): ID пользователя

**Возвращает:** `list` - Список кортежей закладок в порядке от новых к старым

**Структура каждого элемента:**
```
(id, user_id, chat_id, message_id, message_text, author_id, author_name, created_at)
```

**Пример:**
```python
bookmarks = bookmark_manager.get_user_bookmarks(123456)
for bm in bookmarks:
    print(f"Закладка #{bm[0]}: {bm[4]} от {bm[6]}")
```

#### 5. `is_bookmarked(user_id, chat_id, message_id)`
Проверяет, сохранена ли закладка пользователем.

**Параметры:**
- `user_id` (int): ID пользователя
- `chat_id` (int): ID чата
- `message_id` (int): ID сообщения

**Возвращает:** `bool` - `True` если закладка существует, `False` иначе

**Пример:**
```python
is_saved = bookmark_manager.is_bookmarked(
    user_id=123456,
    chat_id=-1001234567890,
    message_id=999
)
if is_saved:
    print("Закладка уже сохранена")
```

## Обработчики сообщений

### `register_bookmarks_handlers(dp)`
Регистрирует все обработчики закладок в dispatcher.

**Параметры:**
- `dp` (Dispatcher): Dispatcher из aiogram

**Использование:**
```python
from modules.bookmarks import register_bookmarks_handlers

# В main_bot.py
@dp.startup
async def on_startup():
    await register_bookmarks_handlers(dp)
```

## Примеры использования

### Пример 1: Получение всех закладок пользователя
```python
bookmarks = bookmark_manager.get_user_bookmarks(user_id=123456)
print(f"Всего закладок: {len(bookmarks)}")

for bookmark_id, user_id, chat_id, msg_id, msg_text, author_id, author_name, created_at in bookmarks:
    print(f"- {msg_text} от {author_name}")
```

### Пример 2: Программное добавление закладки
```python
success = bookmark_manager.add_bookmark(
    user_id=message.from_user.id,
    chat_id=message.chat.id,
    message_id=message.reply_to_message.message_id,
    message_text=message.reply_to_message.text,
    author_id=message.reply_to_message.from_user.id,
    author_name=message.reply_to_message.from_user.username
)

if success:
    await message.reply("✅ Закладка добавлена!")
else:
    await message.reply("⚠️ Эта закладка уже существует")
```

### Пример 3: Очистка старых закладок
```python
from datetime import datetime, timedelta

def cleanup_old_bookmarks(days=30):
    """Удаляет закладки старше N дней"""
    import sqlite3
    cutoff_date = (datetime.now() - timedelta(days=days)).isoformat()
    
    conn = sqlite3.connect(bookmark_manager.db_path, check_same_thread=False)
    cursor = conn.cursor()
    cursor.execute("DELETE FROM bookmarks WHERE created_at < ?", (cutoff_date,))
    conn.commit()
    conn.close()
    
cleanup_old_bookmarks(days=60)
```

## Интеграция с основным ботом

Модуль уже интегрирован в `main/main_bot.py`:

```python
from modules.bookmarks import BookmarkManager, register_bookmarks_handlers

@dp.startup
async def on_startup():
    await register_bookmarks_handlers(dp)
```

## Особенности

✅ **Автоматическое создание таблицы** - при первой инициализации  
✅ **Предотвращение дубликатов** - одно сообщение может быть сохранено только один раз за пользователя  
✅ **Сортировка по времени** - закладки отображаются от новых к старым  
✅ **Поддержка разных типов сообщений** - текст, фото, видео, файлы, стикеры  
✅ **Форвардирование в ЛС** - нажмите на закладку, чтобы получить сообщение в личные сообщения  
✅ **Управление закладками** - удаление прямо из списка

## Тестирование

Запустите тестовый скрипт:
```bash
python test_bookmarks.py
```

Вывод:
```
==================================================
Тестирование модуля закладок
==================================================

✓ Тест 1: Добавление закладки
  Результат: ✅ Закладка добавлена

✓ Тест 2: Проверка наличия закладки
  Результат: ✅ Закладка существует

...
```

## Решение проблем

**Проблема:** Закладка не добавляется  
**Решение:** Убедитесь, что вы ответили именно на сообщение и отправили только эмодзи 📌

**Проблема:** При переходе по закладке получаю ошибку  
**Решение:** Сообщение может быть удалено. Нажмите на других закладку или удалите эту.

**Проблема:** Закладка дублируется при добавлении  
**Решение:** В модуле предусмотрена защита от дубликатов - это невозможно

## Версия

- **Текущая версия:** 1.0
- **Дата:** 20.12.2025
- **Язык:** Python 3.10+
- **Зависимости:** aiogram 2.25.2+, sqlite3

## Лицензия

Часть проекта WERTY | Chat-Manager Bot

---

**Автор:** GitHub Copilot  
**Последняя обновление:** 20.12.2025
